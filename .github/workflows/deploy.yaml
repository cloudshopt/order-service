name: Build & Deploy order-service

on:
  push:
    branches: [dev, main]

permissions:
  contents: read

concurrency:
  group: order-service-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: cloudshopt/infrastructure/.github/workflows/build-image.yaml@main
    with:
      prod_branch: main
      image_repository: timib22/cloudshopt-orders-php
      dockerfile: .docker/php/prod.Dockerfile
      docker_context: .
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  deploy:
    needs: build
    uses: cloudshopt/infrastructure/.github/workflows/deploy-helm.yaml@main
    with:
      prod_branch: main

      infra_repo: cloudshopt/infrastructure
      infra_ref: main

      helm_chart_path: helm/order-service
      values_prod: helm/order-service/values.yaml
      values_dev: helm/order-service/values-dev.yaml

      namespace_prod: cloudshopt
      namespace_dev: cloudshopt-dev
      release_prod: order-service
      release_dev: order-service-dev

      image_repository: timib22/cloudshopt-orders-php
      image_tag: ${{ needs.build.outputs.image_tag }}

      health_prod: http://app.timotejblazic.eu/api/orders/info
      health_dev: http://app-dev.timotejblazic.eu/api/orders/info

      k8s_secret_name: order-service-secrets

    secrets:
      KUBECONFIG_B64: ${{ secrets.KUBECONFIG_B64 }}

      DB_PASSWORD_PROD: ${{ secrets.PROD_DB_PASSWORD }}
      REDIS_PASSWORD_PROD: ${{ secrets.PROD_REDIS_PASSWORD }}
      JWT_SECRET_PROD: ${{ secrets.PROD_JWT_SECRET }}
      DB_PASSWORD_DEV: ${{ secrets.DEV_DB_PASSWORD }}
      REDIS_PASSWORD_DEV: ${{ secrets.DEV_REDIS_PASSWORD }}
      JWT_SECRET_DEV: ${{ secrets.DEV_JWT_SECRET }}